sqlite3 homepage -> downloads-> Precompiled Binaries for Windows (downloaded and extracted to C drive)
->renamed to sqlite3-> environ var edit -> system variable edit path-> add new path 'C:\sqlite3'->done:))))

To Check:
C:\Users\USER>sqlite3
SQLite version 3.51.1 2025-11-28 17:28:25

#type of view in sqlite3
.mode column
.mode markdown
.mode box
.mode table
.mode

INSERT INTO todos (title, description, priority, complete)
VALUES
('design api endpoints', 'define CRUD routes for todos', 2, 0),
('create models', 'write SQLAlchemy models', 1, 0),
('add validation', 'use Pydantic schemas', 2, 0),
('test api', 'test endpoints using Postman', 3, 0),
('write README', 'document project setup and usage', 3, 1);


* later we made the tables using SQLAlchemy, in model.py. owner etc was addded later as fk in the table.

#New year learning yayy!

*while learninh authentication: all endpoints were in main.py. for authorization and making the system scalable we're creating a separate folder for routes named routes. in routes the auth.py is the initialization of APIRoutes. in todo.py we moved all the endpoints used in main.py.and in main.py file we made the chnages.

*ASGI error: def last e oi direcrtory te thakbe jeikhane main.py chilo er theke low level e giye uvicorn main:app --reload dile hobena.

*sql can only create the table not enhance, to do that we need albembic.

*for hashedpass install :pip install bcrypt==4.0.1
:pip install passlib

#in auth, first ei amra db ani nai. jokhon ansi db_dependency inject korsi.(async def e add db:db_dependency type), post e return crreate use model na diye, db.add,db.commit.

*pip install puthon-multipart (authentication - we can submit forms)

#JWT install (authorizatuin)
pip install "python-jose[cryptography]" 

(venv_fastapi) C:\Users\USER\OneDrive\Documents\GitHub\fastAPI_small_lessgo\Todo_app>openssl rand -hex 32
69697acb995d927f038f3fe90e96c2aa25107314cfeeeed508a71545274ed4f7

jwt.io t ewe can decoded header(alg,typ)playload(sub,id,exp),secret.

#Production RDBMS
#Postgresql

login group roles/Postgres(properties->privileges (all selected))=superuser
1.create database (name=TododAppPostgresqlDB)
2.create table (Database/TododAppPostgresqlDB/schemas/tables->(icon-query_tool))

paste this: 

DROP TABLE IF EXISTS users;

CREATE TABLE users (
  id SERIAL,
  email varchar(200) DEFAULT NULL,
  username varchar(45) DEFAULT NULL,
  first_name varchar(45) DEFAULT NULL,
  last_name varchar(45) DEFAULT NULL,
  hashed_pass varchar(200) DEFAULT NULL,
  is_active boolean DEFAULT NULL,
  role varchar(45) DEFAULT NULL,
  PRIMARY KEY (id)
);

DROP TABLE IF EXISTS todos;

CREATE TABLE todos (
  id SERIAL,
  title varchar(200) DEFAULT NULL,
  description varchar(200) DEFAULT NULL,
  priority integer  DEFAULT NULL,
  complete boolean  DEFAULT NULL,
  owner_id integer  DEFAULT NULL,
  PRIMARY KEY (id),
  FOREIGN KEY (owner_id) REFERENCES users(id)
);

NOTICE:  table "users" does not exist, skipping
NOTICE:  table "todos" does not exist, skipping
CREATE TABLE

Query returned successfully in 109 msec.

tables created, now change database url and others in database.py.
then, (venv_fastapi) C:\Users\USER\OneDrive\Documents\GitHub\fastAPI_small_lessgo\Todo_app>pip install psycopg2-binary
then, run uvicorn. after running prev.db will be there incase you want to switch back to sqlite

#MySQL
download:mysql->developer zone->MySQL DOwnloads-> MySQL community server->go to download page->download the istaller
developer Deafault
pass:roza username:admin

to see if the server is there:
C:\Users\USER>services.msc

C:\Users\USER>mysql --version
'mysql' is not recognized as an internal or external command,
operable program or batch file.


okay, version 8 will be downloaded.
Uninstall MySQL 9.5 (important)

Open Control Panel → Programs → Uninstall a program

Uninstall ALL MySQL items, especially:

MySQL Server 9.5

from mysql community downloads downloaded this one- mysql-installer-community-8.0.44.0.msi
mysqlworkbench ->
1.schema crate schema(db)

2. paste in query1
DROP TABLE IF EXISTS `users`;

CREATE TABLE `users` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `email` varchar(200) DEFAULT NULL,
  `username` varchar(45) DEFAULT NULL,
  `first_name` varchar(45) DEFAULT NULL,
  `last_name` varchar(45) DEFAULT NULL,
  `hashed_pass` varchar(200) DEFAULT NULL,
  `is_active` int(1) DEFAULT NULL,
  `role` varchar(45) DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=latin1;

DROP TABLE IF EXISTS `todos`;

CREATE TABLE `todos` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `title` varchar(200) DEFAULT NULL,
  `description` varchar(200) DEFAULT NULL,
  `priority` int(1) DEFAULT NULL,
  `complete` int(1) DEFAULT NULL,
  `owner_id` int(11) DEFAULT NULL,
  PRIMARY KEY (`id`),
  FOREIGN KEY (`owner_id`) REFERENCES users(`id`)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=latin1;

select sql file and check if you getthe clm names

pip install pymysql

#albembic(database migration tool):

models.py if i add phone_num to our db when working in swagger ui while authorozing internal server error 500 will be thrown.

Alembic helps you manage changes to your database schema over time—such as creating tables, adding columns, or modifying constraints—without losing data.
Track schema versions (like Git, but for databases)

-Apply updates incrementally instead of recreating the database

-Rollback changes safely if something breaks

-Keep database schema in sync with your SQLAlchemy models

*Key things Alembic does

1.Initialize a new,hgmneric env
code: alembic init <folder name>

after initialzing two things will appear:
-albembic.ini:congig info
-alembic directory: all env properties for albemic,all rev of application, where you can call the migration for upgrading and downgrading.

2.Creates a revision if the environment
code:alembic revision  -m "msg"

3.Run our upgrade migration to our database:
alembic upgrade <revision #>

4.Run our downgrade migration to our database:
alembic downgrade -1


*gpt
1.Generates migration files (revision files)

2.Applies migrations using:

code:alembic upgrade head


3.Can auto-detect model changes:

code:alembic revision --autogenerate -m "add users table"

*albemic installation:
pip install alembic
alembic init alembic