sqlite3 homepage -> downloads-> Precompiled Binaries for Windows (downloaded and extracted to C drive)
->renamed to sqlite3-> environ var edit -> system variable edit path-> add new path 'C:\sqlite3'->done:))))

To Check:
C:\Users\USER>sqlite3
SQLite version 3.51.1 2025-11-28 17:28:25

#type of view in sqlite3
.mode column
.mode markdown
.mode box
.mode table
.mode

#New year learning yayy!

*while learninh authentication: all endpoints were in main.py. for authorization and making the system scalable we're creating a separate folder for routes named routes. in routes the auth.py is the initialization of APIRoutes. in todo.py we moved all the endpoints used in main.py.and in main.py file we made the chnages.

*ASGI error: def last e oi direcrtory te thakbe jeikhane main.py chilo er theke low level e giye uvicorn main:app --reload dile hobena.

*sql can only create the table not enhance, to do that we need albembic.